{
  "name": "root",
  "version": "1.0.0",
  "repository": "https://github.com/ATJSH/mini-dice-v1",
  "author": "ATJSH <letaem77@gmail.com>",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "apps/*",
      "docs/*",
      "builders/*"
    ]
  },
  "scripts": {
    "clean": "rimraf ./**/dist && rimraf ./**/node_modules",
    "g:tsc": "cd $INIT_CWD && tsc",
    "g:serverless": "cd $INIT_CWD && serverless",
    "packages:build": "yarn workspaces foreach -pvit --from '@packages/*' run build",
    "packages:watch": "yarn workspaces foreach -pvi --from '@packages/*' run watch",
    "server:dev": "yarn workspace @apps/server run dev",
    "server:build": "yarn workspace @builders/server build",
    "server:typecheck": "yarn workspace @apps/server run typecheck",
    "web": "yarn workspace @apps/web"
  },
  "packageManager": "yarn@3.5.0",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.0.0",
    "@typescript-eslint/parser": "^5.0.0",
    "eslint": "^8.0.1",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^4.0.0",
    "prettier": "^2.3.2",
    "rimraf": "^4.4.0",
    "serverless": "^3.28.1",
    "serverless-offline": "^12.0.4",
    "typescript": "^5.0.2"
  },
  "dependenciesMeta": {
    "serverless@3.28.1": {
      "unplugged": true
    },
    "serverless-offline@12.0.4": {
      "unplugged": true
    }
  }
}
